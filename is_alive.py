def is_alive(character):
    """
    Checks if the character is still alive.

    :param character: must be a dictionary key value of "character":""
    :return: True when the character is still alive
    """
    if character["Current HP"] > 0:
        return True


def main():
    """
    Drive the program
    """


if __name__ == '__main__':
    main()
